# FastAPI
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# Pydantic (including settings management)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# LLM Interaction
litellm>=1.30.0 # Use a recent version

# HTTP Requests (used by services)
requests>=2.28.0
websockets>=10.0

# Web Scraping & Related
# Check for latest compatible version
git+https://github.com/salzubi401/crawl4ai.git@main
wikipedia-api>=0.6.0
fasttext>=0.9.2
huggingface-hub>=0.15.0
beautifulsoup4>=4.11.0 # Often needed for HTML processing/cleaning, even if not explicit
lxml>=0.0.0 # Added dependency for potential future XML/HTML parsing needs in scraping
beautifulsoup4>=4.11.0
httpx>=0.24.0

# Reranking
together

#playwright
torch == 2.5.1
